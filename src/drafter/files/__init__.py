"""File-handling support for Drafter.

Submodules provide the environment-aware replacement for the built-in
`open` (`opening`), the Pyodide remote-import patch (`patch_pyodide`),
and a placeholder for future file-system abstraction work (`file_system`).
"""
