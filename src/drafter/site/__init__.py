"""
Outer site structure for a Drafter application.

This package builds the HTML frame that hosts the student's pages: the
`Site` renderer and its templates (`drafter.site.site`), header asset
descriptions such as CSS links (`drafter.site.headers`), and the
`InitialSiteData` container handed to the browser on startup
(`drafter.site.initial_site_data`).
"""
